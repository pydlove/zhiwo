我先有一个零散的人工操作，能否组装成一个流程？

1、生成标题，现在是我点击后设置赛道生成对应数量的标题，然后审核通过后，推送到服务器上。
（因为生成标题需要用到cladue code CLI，而线上版本不能安装，所以在本地版本上操作）；

2、线上版本（也就是服务器上的服务）匹配标题，设置一个推荐日期，根据用户订阅的赛道给用户匹配标题，
如果标题不够，需要线下版本去生成，然后通知管理员审核，审核完成，线上版本继续匹配标题，所以用户的标题都匹配完成后，开始下一阶段；

3、下一阶段就是根据标题和描述生成文章，这个一样依赖本地版本的cladue code CLI，需要让本地版本生成。我先试用标题库的导出功能，
生从一个zip文件，里面包含了所有要生成的文件，一行数据一个Excel， 我本地版本中有一个脚本，
python batch_run.py ${目录的绝对路径}, 这脚本的意思批量执行这个目录下的excel，去生成文章。

4、完成后通知我进行审核文章，如果没有完成就通知我为什么失败了（这个通知可以设置成通过调取公众号的接口提醒，这个我可以找个地方设置需要的参数）


5、管理员审核完成后，应该设置一个推送时间，定时器根据时间自动推送到用户邮箱；
这个过程要记录推送记录，如果失败了也要记录。最终通知用户成功多少，失败多少，这样可以人工接入。


## 问题
1、这里涉及到线上和本地的交互，因为只能单向，我本地能连接线上，因为线上有固定公网ip，而我本地没有；
2、针对这种情况，我们怎么设计交互；
3、一般我是今天准备明天要发送的文章，所以今天得审核完成，设置一个定时明天发送。这个流程应该怎么启动，
我希望尽可能人工介入少，只审核几个关键节点。